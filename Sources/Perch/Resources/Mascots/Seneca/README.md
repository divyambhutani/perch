Seneca sprite assets belong in this directory as:

- `idle.png`
- `watching.png`
- `alert.png`
- `working.png`

The current scaffold keeps the path stable so the runtime loader can switch from fallback symbols to pixel assets without code changes.
